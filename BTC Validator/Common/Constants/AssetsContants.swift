//
//  AssetsContants.swift
//  BTC Validator
//
//  Created by Abhishek kapoor on 16/10/22.
//

import UIKit

enum Constant {
	static let validateImage = UIImage(named: "valid") ?? UIImage()
	static let validateMessage = "Scanned BTC Code is valid"
	
	
	static let invalidateImage = UIImage(named: "invalid") ?? UIImage()
	static let invalidateMessage = "Scanned BTC Code is INVALID"
}
